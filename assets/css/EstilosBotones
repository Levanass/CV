body {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    /* font-family: Arial, sans-serif; /* Añadir una fuente por defecto */
}

.language-buttons {
    position: absolute;
    top: 10px;
    right: 10px;
    display: flex;
    gap: 10px;
}

.language-buttons button {
    background: none;
    border: none;
    padding: 0;
    cursor: pointer;
}

.language-buttons img {
    display: block;
    width: 20px; /* Ajusta el tamaño según sea necesario */
    height: 20px; /* Ajusta el tamaño según sea necesario */
}

button:focus {
    outline: none;
}

